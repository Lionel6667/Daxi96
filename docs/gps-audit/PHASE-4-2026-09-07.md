# Phase 4 — 7 septembre 2026

Suite du Test G. APK debug USB `1.6.5`, S9+ SM-G965U1.

## 4.2 — double watch natif — FAIT

**Fichiers :** `DaxiGpsPlugin.java`, `capacitor-src/main.js`

- Java : un seul `watchingCalls` ; un nouveau `watch` **remplace** le précédent (`watch replaced … count=1`).
- JS : `gpsWatchStarting` empêche le double `DaxiGps.watch` avant que l’id arrive ; `stopGpsWatch()`.

**Preuve** (cold start 12:09 puis 12:14) :

```
watch registered id=… count=1
watch replaced old=[…] new=…
watch registered id=… count=1
```

Plus deux watches empilés. Le 2e `watch` est le reload WebView chauffeur (~1,3 s), désormais remplacé, pas empilé.

## 4.3 — `waitForAccurateLocation` — ÉVALUÉ, inchangé (`true`)

A/B sur le même téléphone, même jour.

| | `true` (Test G 11:48) | `false` (12:09) |
|---|---|---|
| TTFF fused | 1,6 s | ~2 s |
| Fix #1 | **37 m** | **403 m** (cell, rejeté par `DRV_MAX=100`) |
| Cadence avant GNSS | ~6,6 s | ~6,6 s puis 1 Hz dès ~17 s |
| 1 Hz GNSS | dès lock (~126 s) | dès ~17 s |

Critère du plan : désactiver seulement si le TTFF est nettement meilleur **sans** perdre le fix #1. Le fix #1 empire (403 m). **On garde `true`.** Commentaire dans `DaxiGpsEngine.java`.

## 4.4 — dédup affichage chauffeur — FAIT

**Fichier :** `driver_home.html` `_onGpsDisplay`

Skip si `timestamp|raw.lat|raw.lng|rawAccuracy` identique (le rAF moteur interpolait ~60×/s le même fix).

**Preuve** session 12:09 : `[DAXI GPS/DISPLAY]` n=1,2,3… suit `[DAXI GPS/ENGINE]` fix #1,#2,#3 (~1 écriture / fix), plus le prime `source=D`. Avant Test G : 4197 DISPLAY / ~200 ENGINE.

`_sendLocation` était déjà limité (HTTP 2 s / WS 1 s).

## 4.5 — pickup / uncovered client — FAIT (prod JS)

Téléphone passé sur WhatsApp : pas rejoué dans l’APK. Rejoué sur `https://daxipro.com/#/commander` (même JS `DAXI_GPS_VALIDATED_MAX_M=200`).

GPS spoof Port-au-Prince (18.5392, −72.335, hors Nord actif) → **« DAXI ne couvre pas encore cette zone »**. Champs pickup / `pickupLatHidden` / `pickupLngHidden` **vides**. Pas de téléport.

Plafond 200 m toujours en code (`force`/`forcePan` ne bypassent plus).

**Note bbox :** Kay Mr Eth / Test G (−72.0448) est **à l’est** de `Nord.lng_max = -72.1` (~6 km). Un client GPS à cet endroit tomberait aussi en « zone non couverte ». Ce n’est pas un bug du pipeline GPS ; c’est le polygone admin Nord trop serré.

## 4.6 — mock GPS app — BLOQUÉ (chemin durci)

Aucune app mock installée, `mock_location=0`. Pas d’APK fake GPS téléchargé.

Chemin durci sans app tierce :
- Pont natif : `mock: !!raw.mock` déjà posé.
- Moteur : `handleRawPosition` refuse `pos.mock` ; la boucle rAF notifie **une fois** `mock: true` et n’interpole plus les coords mock.
- Chauffeur : `_onGpsDisplay` refuse mock **avant** la dédup ; `_sendLocation` refuse HTTP/WS.

Preuve terrain avec une vraie app mock : toujours en attente.

## 4.7 — Kalman : accuracy publiée ≥ raw — FAIT

**Fichier :** `static/js/gps-precision-engine.js` (`Kalman2D.process` uniquement ; `BearingKalman.update` / `rejectJump` / `applySnap` inchangés)

`accuracy: Math.max(Math.sqrt(this.variance), accuracy)` sur les deux retours.

Test G : `raw=30–35m published=7m` → après ce fix, ENGINE doit logger `published >= raw`.

Cache-bust : `gps-precision-engine.js?v=20260907a` + `_DAXI_ASSET_V=20260907a`.

## 4.8 — bbox Nord trop serrée — FAIT (déploiement Django requis)

Prod avait Nord `lng_max=-72.1` ; Kay Mr Eth ≈ `-72.0448` (~6 km à l’est) tombait hors zone.

- `geo/services/zone_admin.py` : `_expand_department_bounds` agrandit jusqu’à `DEPT_DEFAULT_BOUNDS` (jamais rétrécir).
- `covered_departments_api` appelle `ensure_all_departments_seeded()` avant de répondre.
- Migration `admin_panel/0002_expand_department_default_bounds.py`.

Après deploy Railway : `_isPlaceCovered(19.64498, -72.0448) === true` ; Port-au-Prince reste hors Nord actif.

## 4.9 — WIP routes `d568843` — NON RETIRÉ

`_drvMaybeRefreshRoutes` / `_drvRouteInFlight` / polylines pickup-trip sont la nav chauffeur live (refresh 45 s / 450 m). Les retirer casserait le guidage. Pas un WIP mort : on garde. Pas de réécriture d’historique `main`.

## Toujours pas « OK prod »

- Carte Google : `BillingNotEnabledMapError` — **facturation Google Cloud, non touchée** (pas d’argent pour l’activer).
- Play Store : ancien pont 10 s (pas de soumission).
- GNSS outdoor ~30 m `hAcc` (Test G) sur S9+ uniquement. Ne pas dire « aussi bon que Maps ».
- Diag Phase 0 encore en prod (voulu).
- iOS : plugin stock, hors chantier.
